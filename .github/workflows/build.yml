name: Build and publish

on: 
  push:

jobs:
  build_pages:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    steps:
    - name: Perform Checkout
      uses: actions/checkout@v2
    - name: Install Saxon, ANT and ANT-Contrib
      run: |
        apt-get update && apt-get install openjdk-11-jre-headless ant ant-contrib -y --no-install-recommend
        wget https://sourceforge.net/projects/saxon/files/Saxon-HE/9.9/SaxonHE9-9-1-7J.zip/download && unzip download -d saxon && rm -rf download
    - name: Download Fundament
      run:
        ./dl_fundament.sh
    - name: Build
      run: |
        ant
    - name: StaticSearch
      run:
        ./dl_staticsearch.sh
    - name: Build Search Index
      run:
        ant -f ./static-search/build.xml -DssConfigFile=${PWD}/ss_config.xml
    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./html